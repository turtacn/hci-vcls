// Package qm provides an abstraction layer for interacting with QEMU Server (`qm`).
// This package is responsible for starting, stopping, and checking the status
// of Virtual Machines in the HCI vCLS cluster. It does not dictate which VM
// to manage, but rather exposes a common interface for the execution engine.
package qm

